# 2026-03-17

## QMD 路径修复
- 背景：仓库目录迁移后，`qmd` 默认检索未覆盖当前项目。
- 动作：新增 collection `react-debug-inspector`，路径指向 `/Users/linhey/Desktop/linhay-open-sources/react-debug-inspector`。
- 验证：`qmd query "react debug inspector" -c react-debug-inspector` 返回当前仓库文档。
- 后续：跨项目检索时建议显式加 `-c react-debug-inspector`。

## Git 与 QMD 收尾
- Git 恢复：将仓库恢复为本地独立 `.git` 目录，修复目录迁移后的 gitdir 失效问题。
- QMD 收敛：移除非当前项目 collection，仅保留 `react-debug-inspector`。
- 验证：`git status` 正常；`qmd collection list` 仅 1 个集合；`qmd query "github pages"` 命中当前仓库文档。
