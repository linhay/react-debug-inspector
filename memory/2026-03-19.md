# 2026-03-19

## 关键决策
- 针对 GitHub issue #8（Next.js Turbopack 配置问题）确认官方推荐写法：`.babelrc` 中使用 `"module:@linhey/react-debug-inspector"`，避免 Babel 按命名规则错误解析为 `@linhey/babel-plugin-react-debug-inspector`。
- 在 README 中新增 Next.js（含 Turbopack）接入章节，并提供 App Router 下客户端初始化 `initInspector()` 的参考实现。

## 行动项与落地
- 新增功能规格文档：`docs-dev/features/nextjs-turbopack-integration-spec.md`。
- 新增 TDD 约束测试：`src/nextjs-turbopack-config.test.ts`（先失败后通过）。
- 新增 demo 工程：`examples/next-turbopack/`，包含 `.babelrc`、`app/layout.tsx`、`app/page.tsx`、`app/inspector-client.tsx`、`README.md`。

## 验证结论
- `npm test` 全部通过（81 tests）。
- `npm run build` 通过。

## 补充里程碑（Pages Router Demo）
- 新增 `examples/next-pages-router` 可运行示例工程，覆盖旧版 Next.js `pages/` 路由接入。
- 新增规格文档：`docs-dev/features/nextjs-pages-router-demo-spec.md`。
- 新增测试：`src/nextjs-pages-router-demo-config.test.ts`。
- 实测 `npm run dev` + `curl` 返回 200，且页面出现 `data-debug` 注入。
