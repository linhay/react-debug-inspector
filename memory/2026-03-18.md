# 2026-03-18

## Demo 视频录制链路落地
- 背景：本期需求是产出可复用的操作示例视频，主题为“使用 debug 工具选择元素后粘贴到输入框”。
- 决策：新增演示专用 Playwright 配置与用例，固定流程并启用视频输出，避免手工录屏不稳定。
- 动作：
  - 新增 `docs-dev/features/demo-video-recording-spec.md`（BDD 场景与验收）
  - 新增 `playwright.demo.config.ts`（端口 `4173`，`video: on`）
  - 新增 `e2e/demo-recording.spec.ts`（包含可见鼠标覆盖层与选择-粘贴流程）
  - 新增脚本：`npm run record:demo` / `npm run record:demo:headed`
- 验证：
  - `npm test -- src/demo-recording-config.test.ts` 通过
  - `npm run record:demo` 通过
  - 视频产物：`test-results/demo-video/demo-recording-Demo-视频录制-应演示选择元素并粘贴到输入框（含可见鼠标）-chromium-demo/video.webm`

## Demo 视频脚本加长（多步骤）
- 背景：反馈认为演示流程过短，需要覆盖更多操作步骤。
- 动作：将演示脚本升级为多段流程：进入检查模式、复制标题 debug id 并粘贴、复制图片信息并粘贴、全部复制并粘贴、最后退出检查模式。
- 可视化：继续保留并强化“可见鼠标”覆盖层，确保移动与点击在视频中清晰可见。
- 验证：`npm run record:demo` 通过（`1 passed`，约 14.6s）。
- 交付：更新稳定输出路径
  - `assets/demo/20260318-debug-select-and-paste-with-cursor.webm`
  - `assets/demo/20260318-debug-select-and-paste-with-cursor.mp4`
