
## 2026-03-10 Pages 文档与测试补全（重试）
- 补齐 Pages BDD 规格、测试策略与自动化回归测试。
- 新增 `src/pages-config.test.ts`（5 条断言）覆盖工作流/文档/链接一致性。
- 工作流改为 `npm ci` 并增加 test-app lockfile 缓存键。
- README 与 GITHUB_PAGES 文档改为真实演示地址与完整排障步骤。
- 验证：`npm test -- src/pages-config.test.ts` 通过。

## 2026-03-10 移除分支保护并直合
- 用户要求：移除 main 分支保护并直接合并 PR。
- 动作：删除 `main` 分支保护（DELETE /branches/main/protection），随后执行 `gh pr merge 3 --squash --delete-branch --admin`。
- 结果：PR #3 已合并，merge commit `c2697a62ba2369e61fc9f0a7b2f458b7b77a400d`。

## 2026-03-10 Pages 卡顿修复
- 现象：Pages 演示页在检查模式下鼠标移动卡顿。
- 原因：`runtime.ts` 在每次 mousemove 事件都直接执行 overlay/tooltip DOM 更新。
- 修复：将 hover 更新改为 requestAnimationFrame 批处理（每帧最多一次），并将 mousemove 监听挂到 document 以匹配冒泡路径。
- 验证：`npm test -- src/runtime.test.ts src/runtime-advanced.test.ts` 通过（15/15）。

## 2026-03-10 卡顿二次优化与 agent-browser 阻塞
- 现象：agent-browser 在当前环境 `open/snapshot` 会话阻塞（daemon/socket 异常），无法稳定产出交互快照。
- 优化：runtime 增加 hover RAF 批处理、同元素重复 hover 跳过更新；anchor 更新改为 RAF 调度。
- 兼容：调整 runtime 单测等待时序与阻塞场景 mock，保持行为断言稳定。
- 验证：`npm test -- src/runtime.test.ts src/runtime-advanced.test.ts` 通过（15/15）。

## 2026-03-10 Pages 性能修复上线与 CI 矩阵修正
- 推送提交 `e101e33`，GitHub Pages 部署成功（run: 22892952272）。
- 线上资源已更新为 `assets/index-CPoTYduJ.js`，用于承载 hover/anchor 的 RAF 性能优化。
- 发现 CI 在 Node 18 上触发 `ERR_REQUIRE_ESM`（vite/vitest 配置加载失败）。
- 采用 TDD 修复：先在 `pages-config.test.ts` 增加 Node 20+ 约束测试（先失败），再将 `.github/workflows/ci.yml` 矩阵改为 `[20.x, 22.x]`。
- 验证：`npm test -- src/pages-config.test.ts` 与 `npm test` 全部通过（53/53）。

## 2026-03-10 CI E2E 失败修复（嵌套组件用例）
- 线上 CI 失败原因：`e2e/inspector.spec.ts` 的 `should handle nested components correctly` 使用了过时选择器（`.card h2` 与 `.reset-button`）。
- 现状对齐：demo 页面实际为 `.card h3`，重置按钮为 `button` 且文本 `Reset Counter`。
- 修复：改为稳定语义定位（`.card h3` + `getByRole('button', { name: 'Reset Counter' })`），并更新对应 `data-debug` 断言。
- 验证：`CI=1 npm run test:e2e` 通过（10/10）；`npm test` 通过（53/53）。

## 2026-03-10 Demo 页面增加 GitHub 返回入口
- 需求：Pages demo 页面需要能跳回 GitHub 仓库。
- BDD：更新 `docs-dev/features/github-pages-spec.md`，新增“场景 4：Demo 页支持跳回 GitHub”。
- TDD：先在 `src/pages-config.test.ts` 增加断言（入口文案/仓库 URL/`target="_blank"`），首轮执行失败（红灯）。
- 实现：在 `test-app/src/App.tsx` header 中新增“返回 GitHub”链接，并在 `test-app/src/App.css` 增加按钮样式。
- 验证：`npm test -- src/pages-config.test.ts` 通过。

## 2026-03-10 Pages 文档目录收拢
- 需求：将 Pages 相关文档整理到独立目录。
- 调整：新增 `pages/` 目录，并将以下文件迁移到该目录：
  - `GITHUB_PAGES.md` -> `pages/GITHUB_PAGES.md`
  - `docs-dev/features/github-pages-spec.md` -> `pages/github-pages-spec.md`
  - `docs-dev/dev/github-pages-test-strategy.md` -> `pages/github-pages-test-strategy.md`
- 同步：更新 `README.md`、`FINAL_SUMMARY.md`、`src/pages-config.test.ts` 及 pages 文档内部交叉引用。
- 验证：`npm test -- src/pages-config.test.ts` 通过（7/7）。

## 2026-03-10 Gemini 驱动的 Pages 重设计
- 需求：让 Gemini 重新设计 GitHub Pages demo 页面视觉层。
- 方式：使用 `gemini -p` 生成新 UI 草案，再在仓库内按现有测试约束落地实现。
- BDD：更新 `pages/github-pages-spec.md`，新增“场景 5：Demo 页视觉重设计”。
- TDD：在 `src/pages-config.test.ts` 新增 Hero 分区布局断言（先红后绿）。
- 实现：重写 `test-app/src/App.tsx` 与 `test-app/src/App.css`，保留原有交互行为与关键测试选择器/文案（Counter/Increment/Reset/Open Dialog/Close/返回 GitHub）。
- 验证：
  - `npm test -- src/pages-config.test.ts` 通过（8/8）。
  - 由于本机 `:5173` 被外部服务占用，使用临时 `playwright.local.config.ts` 在 `:4173` 端口执行 E2E，`npx playwright test --config playwright.local.config.ts` 通过（10/10）。

## 2026-03-10 npm 发布尝试与阻塞
- 动作：执行 `npm test`（55/55 通过）与 `npm run build`（通过）后，尝试发布 `@linhey/react-debug-inspector@1.1.0`。
- 结果：`npm publish --access public` 失败，返回 `E404`（包不存在或无权限）。
- 诊断：
  - `npm whoami` 返回 `401 Unauthorized`，当前机器未完成 npm 登录。
  - 作用域包 `@linhey/react-debug-inspector` 发布权限当前不可用（未登录或账户不具备该 scope 权限）。
- 风险结论：代码已可发布，但需先恢复 npm 认证与 scope 权限，才能完成实际上架。

## 2026-03-10 Release 与 NPM 自动发布链路修复
- 现象：`Version Bump and Release` 首次执行失败（release.yml 未安装 Playwright 浏览器）；修复后 release 成功创建 `v1.2.0`，但 `Publish to NPM` 未自动触发。
- 根因：
  - `release.yml` 在 `npm run test:all` 前缺少 `npx playwright install --with-deps chromium`。
  - `publish.yml` 仅监听 `release: created`，而由 `GITHUB_TOKEN` 触发的上游工作流创建 release 不会触发下游工作流（防递归机制）。
- 修复：
  - `release.yml` 新增 Playwright 浏览器安装步骤。
  - `publish.yml` 新增 `workflow_dispatch` 与 `workflow_run (Version Bump and Release completed)` 触发条件，并加入成功条件判断。
  - 新增测试 `src/release-config.test.ts` 与 `src/publish-config.test.ts` 覆盖关键发布配置。
- 验证：新增工作流配置测试通过；release 工作流二次执行成功，产出 GitHub Release `v1.2.0`。

## 2026-03-10 参考项目登记（click-to-component）
- 需求：将 `https://github.com/ericclemmons/click-to-component` 列为参考项目，并在 Git 中忽略本地副本。
- 实现：
  - 新增 `references/README.md`，登记参考仓库与本地建议目录。
  - 更新 `.gitignore`：忽略 `references/*`，仅保留 `references/README.md` 可追踪。

## 2026-03-10 Trusted Publishing 切换
- 目标：移除 `NPM_TOKEN` 依赖，改用 npm Trusted Publishing（GitHub OIDC）。
- 代码改动：
  - `publish.yml` 移除 `NODE_AUTH_TOKEN` / `secrets.NPM_TOKEN`，保留 `id-token: write` + `npm publish --provenance --access public`。
  - 新增 `src/publish-config.test.ts` 断言 Trusted Publishing 关键约束（禁止出现 `NODE_AUTH_TOKEN`）。
- 文档改动：`GITHUB_ACTIONS.md` 与 `RELEASE_CHECKLIST.md` 改为 Trusted Publishing 指引。
- 仓库配置：已删除 GitHub Secret `NPM_TOKEN`（避免继续走 token 发布）。
- 备注：npm 侧仍需完成 Trusted Publisher 绑定后，`Publish to NPM` 才会成功。

## 2026-03-10 Trusted Publishing 再验证（仍阻塞）
- 用户确认已完成 npm Trusted Publisher 绑定后，复跑 `Publish to NPM`：`22899137176`、`22899655411`。
- 结果：两次均在 `npm publish --provenance --access public` 步骤失败，错误一致：`E404 Not Found - PUT https://registry.npmjs.org/@linhey%2freact-debug-inspector`。
- 交叉核对：
  - 仓库 remote：`git@github.com:linhay/react-debug-inspector.git`（owner 为 `linhay`）。
  - 包名：`@linhey/react-debug-inspector`。
  - 包现状：`npm view @linhey/react-debug-inspector version` 返回 `1.0.0`（`1.2.0` 尚未发布）。
- 风险结论：CI 与包权限本身无明显构建问题，阻塞点仍在 npm 侧 Trusted Publisher 绑定匹配（owner/repo/workflow/context 任一不一致都会触发该类 404）。
- 复验：用户确认已修正 Trusted Publisher 后，再次触发 `Publish to NPM`（run `22899779012`），仍在发布步骤失败，错误同前：`E404 Not Found - PUT https://registry.npmjs.org/@linhey%2freact-debug-inspector`。
- 结论未变：npm Trusted Publisher 绑定仍未与当前 workflow identity 完全匹配。
- Trusted Publishing 根因最终定位：
  1) GitHub Runner 默认 `npm 10.8.2`，`setup-node` 默认注入 token，未走稳定的 Trusted Publishing 认证路径；
  2) 升级 npm 后报错升级为 `E422`，暴露包元数据缺失：`package.json.repository.url` 为空，导致 provenance 校验与仓库不匹配。
- 修复：
  - `publish.yml`：`setup-node` 增加 `token: ''`，新增 `npm install -g npm@^11`，发布前 `unset NODE_AUTH_TOKEN`。
  - `package.json`：补充 `repository.type=url` 并指向 `https://github.com/linhay/react-debug-inspector`。
  - 测试：`src/publish-config.test.ts` 新增/更新断言覆盖上述约束。
- 验证：`Publish to NPM` run `22908613615` 成功；`npm view @linhey/react-debug-inspector version` = `1.2.0`。

## 2026-03-10 移除 inspector 悬浮开关拖拽
- 需求：推送当前 runtime 改动，并保持测试与记忆同步。
- 调整：删除 `src/runtime.ts` 中悬浮开关的拖拽状态与 mousemove/mouseup 处理，仅保留点击切换与弹窗事件隔离。
- 测试：将 `src/runtime.test.ts` 改为断言 mousedown 后的 pointer move 不再改变按钮定位。
- 验证：`npm test` 通过（6 个测试文件，60 个用例）。

## 2026-03-10 Inspector 扩展复制能力
- BDD：新增 `docs-dev/features/inspector-copy-spec.md`，定义检查模式下的菜单复制行为（复制 ID / 文案 / 图片 / 全部复制）。
- TDD：先扩展 `src/runtime-advanced.test.ts` 与 `e2e/inspector.spec.ts`，覆盖菜单显隐、文案提取、图片二进制与降级复制、结构化“全部复制”。
- 实现：`src/runtime.ts` 增加悬浮操作菜单、文本提取、图片解析/降级复制、结构化 payload 构建，并将 `initInspector()` 改为可清理的单例初始化，避免重复注册全局监听器。
- Demo：`test-app/src/App.tsx` / `test-app/src/App.css` 新增 Copy Showcase 区块，加入可复制文案和可复制图片示例。
- 文档：`README.md` 更新为菜单复制交互说明。
- 验证：`npm test` 通过（8 个测试文件，68 个用例）；`npx playwright test --config /tmp/playwright.local.config.ts` 在本地 `:4173` 临时配置下通过（12/12）。
- 环境说明：默认 `playwright.config.ts` 复用本机已有 `:5173` 服务，会命中外部页面；本次 E2E 采用临时 `:4173` 独立服务规避端口污染。

## 2026-03-10 Pages 增加 agent-browser 测试案例
- 需求：使用 `agent-browser` 对 Pages demo 做实际验证，并在 `pages/` 提供可复用案例。
- TDD：先在 `src/pages-config.test.ts` 增加文档门禁，要求存在 `pages/AGENT_BROWSER_CASES.md` 且包含线上/本地案例与复制菜单命令。
- 实测：
  - 本地 `http://127.0.0.1:4173/`：`agent-browser snapshot -i` 可识别 `返回 GitHub / Increment / Reset Counter / Open Dialog / 🎯`。
  - 进入检查模式并 hover 后，可识别 `复制 ID / 复制文案 / 复制图片 / 全部复制`。
  - 打开 Dialog 后再次 hover `Close`，复制菜单仍可出现。
  - 线上 `https://linhay.github.io/react-debug-inspector/` 也可完成基础元素探测。
- 文档：新增 `pages/AGENT_BROWSER_CASES.md`，并在 `pages/GITHUB_PAGES.md` / `pages/github-pages-test-strategy.md` 增加引用。
- 验证：`npm test -- src/pages-config.test.ts` 通过；`npm test` 通过（8 个测试文件，69 个用例）。
- 环境结论：默认 `:5173` 容易复用外部现存服务，agent-browser 本地案例统一建议使用 `127.0.0.1:4173`。
